# Runtime dependencies for EnvSyncer.
# For a normal install use `pip install .` (reads pyproject.toml).
# This file exists for `pip install -r requirements.txt` dev workflows.
typer>=0.12
rich>=13.7
requests>=2.31
pathspec>=0.12

# Dev / test only:
pytest>=8.0
responses>=0.25
